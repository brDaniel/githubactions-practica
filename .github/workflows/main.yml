name: Test

on: [push]

jobs:
    test-build:
        runs-on: ubuntu-latest
        
        steps:
            - name: Checkout
              uses: actions/Checkout@v2

            - name: ls
              run: |
                chmod +x script.sh
                ./script.sh

            - name: lss
              uses: ./.github.actions/my-action