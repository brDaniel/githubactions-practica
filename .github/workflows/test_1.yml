name: Hola Mundo

on: [push]

jobs:
  hola:
    runs-on: ubuntu-latest
    steps:
      - name: hola mundo
        run: echo "holamundo"
  
  other:
    runs-on: ubuntu-latest
    steps:
      - name: LS
        run: ls -la